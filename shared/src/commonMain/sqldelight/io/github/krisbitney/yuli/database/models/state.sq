CREATE TABLE state (
    id INTEGER PRIMARY KEY,
    isLoggedIn INTEGER AS Boolean NOT NULL,
    isLocked INTEGER AS Boolean NOT NULL,
    lastUpdate INTEGER NOT NULL
);

select:
SELECT *
FROM state;

replace:
REPLACE INTO state(id, isLoggedIn, isLocked, lastUpdate)
VALUES ?;