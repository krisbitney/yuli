CREATE TABLE user (
    username TEXT PRIMARY KEY,
    name TEXT NOT NULL,
    picUrl TEXT NOT NULL,
    followerCount INTEGER NOT NULL,
    followingCount INTEGER NOT NULL
);

select:
SELECT *
FROM user;

replace:
REPLACE INTO user(username, name, picUrl, followerCount, followingCount)
VALUES ?;